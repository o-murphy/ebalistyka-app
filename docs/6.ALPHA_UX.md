# Alpha UX — TODO

> **Doc #6 — Status: IN PROGRESS 🔄**  
> Remaining: `DistanceConvertorScreen`, filter panel for lists/collections.

Features needed before or immediately after the first alpha release.

---

- [ ] **Filter panel** — My Weapon / My Sight / My Ammo lists and their collection screens (currently filter button is a stub)
  - **Weapon** — filter by: vendor
  - **Ammo** — filter by: vendor, bullet weight (range via `UnitConstrainedField`)
  - **Sight** — filter by: vendor, focal plane (FFP / SFP)
  - **UI** — top sheet or side drawer; scrollable `ListView` with `ExpansionTile` spoilers per category
    - Categorical values: checkboxes for multi-select
    - Weight: `UnitConstrainedField` (min / max range)
    - Each value tile shows a `Badge` with the item count matching that value

- [ ] `DistanceConvertorScreen` — implement (currently stub)
  - Add to unit settings entity and its screen a **target size unit** (cm / inch, default: inch); add FC and UnitFormatter for this value
  - Inputs:
    - Target physical size with unit selector (cm / inch)
    - Target angular size with unit selector (MOA / MRAD / MIL / cm/100m / in/100yd)
  - Output: distance in Meter / Yard / Foot
  - Formula: `distance = physicalSize / angularSize` (same dimensions — angular in radians, physical in meters)
  - **ReticleView embedded** — shows the reticle with the target SVG overlay sized to the input angular value; allows visual verification of the result. Uses the same `ReticleView` widget as `ReticleViewScreen` (shared, post-alpha).
  - Note: `ReticleViewScreen` uses the inverse direction — given `conditions.targetDistance` + physical size → derives angular size in MIL for display. The formula and `ReticleView` widget are shared between both screens.
- [x] `VelocityConvertorScreen` — mps / km·h / fps / mph / Mach input; Mach uses ICAO or custom atmosphere (temperature, pressure, humidity, altitude)
- [x] Settings → About — GitHub link
- [x] Export / Import profile — .ebcp (eBalistyka) and .a7p (Archer); auto-detect format on import
- [x] Export / Import backup — Settings → "Export backup" / "Import backup" (.ebcp, full data)
