Виправ доку

## Спільні використані об'єкти (Reference Sharing)
- **Не можна видалити** Rifle/Cartridge/Sight, якщо він у використанні хоч одним профілем.
Насправді тільки райфл не можна видаляти якщо він in use

Також я насправді не певний в концепціїтримати посилання, поки ми не використовуємо ні hive ні isar, Я не працював з цими бд і не знаю чи це не буде складно зробити. Може треба простішу концепцію, без проміжного стану myrifles, mybullets
Тобто є два шляхи і я не знаю який обрати:
1. один профіль - одна гвинтівка - можна заряджати різними патронами, відповідно посилання, юзерська бібліотека,тільки вбудована колекція
2. один профіль - одна гвинтівка - один патрон, ніяких окремих юзер бібліотек, тільки вбудована колекція

Від цього також залежатиме Flow Branches 

## Wizard Screen — Концепція (Повторно використовуваний)

* Дозволяє редагувати всі поля перед збереженням - не всі а визначені

## Shared UI Components

**Tile поведінка:**
- Якщо `builtin: true` → **без cog btn**
- Якщо `builtin: false` (user data) → показувати **cog btn** (⚙️) для налаштувань (edit / duplicate / delete)
- Tap на tile (без cog) → вибір / копіювання в wizard - насправді тап на тайл нічого не робитиме, булде нопка select