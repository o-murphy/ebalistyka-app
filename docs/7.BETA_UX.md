# Beta Features

> **Doc #7 — Status: BACKLOG 📋**  
> Features for after alpha release. Architecture TBD for each item.

---

## Done ✅

- [x] **.a7p import/export** — `packages/a7p` (`A7pFile`, `A7pConverter`, `A7pValidator`, proto-scheme); `A7pService` in `lib/core/services/`
- [x] **Profile export** — bottom sheet with format selection: .ebcp / .a7p
- [x] **Profile import** — single FilePicker (.ebcp + .a7p), auto-detect by extension
- [x] **Full backup** — Settings → "Export backup" / "Import backup" (.ebcp)
- [x] **Reticle Fullscreen Screen** — `ReticleViewScreen`: `ZoomableView`, barrel drum adjustments, target/reticle pickers, clicks settings
- [x] **RulerSelector** — implemented in `QuickActionsPanel`

---

## Ballistics & UI

- [x] **Display corrections in clicks** — Home Page 1 (AdjPanel) ✅, Tables screen ✅, ReticleViewScreen ✅
- [x] **Enter corrections in clicks** — ReticleViewScreen ✅, AmmoWizardScreen - allow to enter corrections in clicks
- [x] **ammo.zeroOffset** — fields `zeroOffsetX` / `zeroOffsetY` / `zeroOffsetUnitX` / `zeroOffsetUnitY` in `Ammo`, UI in `AmmoWizardScreen`, taking into account in calculations
- [ ] **Custom drag function editor** — complete custom drag function editor (`CustomDragTableEditor`): now read-only by default; add full editing, validation, saving
- [ ] **Known bug** - overlap on android on keyboard open

---

## Filter Panel

- [ ] **Filter panel** — My Weapon / My Sight / My Ammo lists and their collection screens (currently filter button is a stub)
  - **Weapon** — filter by: vendor
  - **Ammo** — filter by: vendor, bullet weight (range via `UnitConstrainedField`)
  - **Sight** — filter by: vendor, focal plane (FFP / SFP)
  - **UI** — top sheet or side drawer; scrollable `ListView` with `ExpansionTile` spoilers per category
    - Categorical values: checkboxes for multi-select
    - Weight: `UnitConstrainedField` (min / max range)
    - Each value tile shows a `Badge` with the item count matching that value

---

## Entity Images

- [ ] Format and storage for `entity.image` (Weapon/Sight/Ammo) — TBD (filesystem/base64/asset)
- [ ] Image picker / camera UI in wizard screens (Weapon, Sight, Ammo)
- [ ] `SightWizardScreen._SightPlaceholder` — replace `Icon(IconDef.sight)` with SVG reticle from `reticleSvgProvider` (similar to `_RiflePlaceholder` in WeaponWizard)

---

## Code Quality

- [ ] **Optimization and restructuring** — unification of components, reusable components; audit of common patterns between screens

---

## Localization (EN / UA)

- [x] ARB + `flutter_localizations`
- [~] Languages: Ukrainian (main) + English — 180 keys, EN = UK (fully in sync)
  - [x] Settings screen (+ sub-screens: units, adjustments)
  - [x] Convertors screen (all sub-screens)
  - [x] Conditions screen
  - [x] HTML trajectory report
  - [x] Tables screen
  - [x] Tables config screen
  - [x] Trajectory table widget
  - [x] Details table widget
  - [ ] Home screen
  - [ ] Profiles screen (`my_profiles_screen`)
  - [ ] My Ammo / My Sights / My Weapons screens
  - [ ] Ammo wizard / Weapon wizard / Sight wizard
  - [ ] Reticle view screen
  - [ ] Shot details screen
  - [ ] Collection screens (ammo, sight, weapon pickers)

---

## Platform & Build

- [x] **Android APK** — configure the build (signing, release flavors)
- [ ] **macOS** — build for macOS
- [ ] **iOS** — build for iOS (C++ `.a` static lib in Xcode)
- [x] **Windows** — package with msix and sign the application for distribution
- [x] **CI/CD** — release workflow: builds all platforms, publishes GitHub Release with all assets
- [ ] **Auto-update** — auto-update (desktop and mobile)
  - [ ] **Linux AppImage** — appimageupdate with zsync via github releases 
    > [!WARNING] 
    > autoupdate is untested end-to-end
  - [ ] **Windows msix** — `.appinstaller` native auto-update via GitHub Releases latest
    > [!WARNING] 
    > windows says that `.appinstaller` is invalid
  - [ ] **Android apk** — ota_update
  - [ ] **macOS** — flutter auto_updater or modern desktop_updater
  - [ ] **iOS ipa** — only app-store, no autoupdates via sideload
- [ ] Possibly in future
  - [ ] **Linux portable/snap/flatpak** - with autoupdates?
  - [ ] **Windows portable** - add autoupdates?
  - [ ] **Android apk** — GooglePlay ?
  - [ ] **iOS ipa** — App Store ?
---

## Reliability

- [x] **Database resilience** — `_openStore` catches open failures, deletes `data.mdb`/`lock.mdb`, re-inits fresh store; SnackBar warning shown only when prior data existed

---

## UX / Features

- [ ] **Notepad** — Note button on Home (currently `showNotAvailableSnackBar`)
- [ ] **Help Overlay** — Coach marks on first run or upon user request; library TBD (e.g. `tutorial_coach_mark`)
- [ ] **Tools Screen** — Opened from "More" button on Home; composition TBD
- [ ] **Settings — Legal links** — Privacy Policy, Terms of Use, Changelog
