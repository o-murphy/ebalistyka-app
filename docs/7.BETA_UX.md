# Beta Features

> **Doc #7 — Status: BACKLOG 📋**  
> Features for after alpha release. Architecture TBD for each item.

---

## Done ✅

- [x] **.a7p import/export** — `packages/a7p` (`A7pFile`, `A7pConverter`, `A7pValidator`, proto-scheme); `A7pService` у `lib/core/services/`
- [x] **Profile export** — bottom sheet with format selection: .ebcp / .a7p
- [x] **Profile import** — single FilePicker (.ebcp + .a7p), auto-detect by extension
- [x] **Full backup** — Settings → "Export backup" / "Import backup" (.ebcp)
- [x] **Reticle Fullscreen Screen** — `ReticleViewScreen`: `ZoomableView`, barrel drum adjustments, target/reticle pickers, clicks settings
- [x] **RulerSelector** — реалізовано у `QuickActionsPanel`

---

## Ballistics & UI

- [ ] **Відображення поправок в кліках** — Home Page 1 (AdjPanel), Tables screen, ReticleViewScreen
- [ ] **ammo.zeroOffset** — поля `zeroOffsetX` / `zeroOffsetY` у `Ammo`, UI у `AmmoWizardScreen`, врахування у розрахунках
- [ ] **Custom drag function editor** — завершити редактор кастомних drag-функцій (`CustomDragTableEditor`): зараз read-only за замовчуванням; додати повноцінне редагування, валідацію, збереження

---

## Filter Panel

- [ ] **Filter panel** — My Weapon / My Sight / My Ammo lists and their collection screens (currently filter button is a stub)
  - **Weapon** — filter by: vendor
  - **Ammo** — filter by: vendor, bullet weight (range via `UnitConstrainedField`)
  - **Sight** — filter by: vendor, focal plane (FFP / SFP)
  - **UI** — top sheet or side drawer; scrollable `ListView` with `ExpansionTile` spoilers per category
    - Categorical values: checkboxes for multi-select
    - Weight: `UnitConstrainedField` (min / max range)
    - Each value tile shows a `Badge` with the item count matching that value

---

## Entity Images

- [ ] Формат і сховище для `entity.image` (Weapon/Sight/Ammo) — TBD (файлова система / base64 / asset)
- [ ] Image picker / camera UI у wizard screens (Weapon, Sight, Ammo)

---

## Code Quality

- [ ] **Оптимізація і реструктуризація** — уніфікація компонентів, реюзабл компоненти; аудит спільних патернів між екранами

---

## Localization

- [ ] ARB + `flutter_localizations`
- [ ] Languages: Ukrainian (main) + English
- [ ] Covers all UI lines: unit names, validation errors, labels

---

## Platform & Build

- [ ] **Android APK** — налаштувати збірку (signing, release flavors)
- [ ] **macOS** — збірка під macOS
- [ ] **iOS** — збірка під iOS (C++ `.a` static lib у Xcode)
- [ ] **Windows** — підпис додатку для дистрибуції
- [ ] **Auto-update** — автооновлення (десктоп і мобайл)

---

## Reliability

- [ ] **Database resilience** — спеціальна поведінка у випадку пошкодження OB бази: відкрити резервний шлях / повідомити користувача / відновити з backup

---

## UX / Features

- [ ] **Нотатник** — Note button on Home (currently `showNotAvailableSnackBar`)
- [ ] **Help Overlay** — Coach marks on first run or upon user request; library TBD (e.g. `tutorial_coach_mark`)
- [ ] **Tools Screen** — Opened from "More" button on Home; composition TBD
- [ ] **Settings — Legal links** — Privacy Policy, Terms of Use, Changelog
