# Beta Features

> **Doc #7 — Status: BACKLOG 📋**  
> Features for after alpha release. Architecture TBD for each item.

---

## What to do next

Priority: 🔴 High · 🟡 Medium · 🟢 Low  
Effort: S < 1 day · M 2–3 days · L 1+ week · XL multi-week

| # | Item | Priority | Effort | Notes |
|---|------|----------|--------|-------|
| 3 | **Custom drag function editor** — full edit/save | 🟢 | M | Read-only scaffold already exists (`CustomDragTableEditor`); UX needs rethink |
| 4 | **`_SightPlaceholder` → SVG reticle** | 🟢 | S | Replace `Icon(IconDef.sight)` with SVG from `reticleSvgProvider`; pattern in `_RiflePlaceholder` |
| 6 | **macOS build** | 🟡 | M | CI pattern exists from Linux/Windows; FFI + code signing |
| 7 | **Notepad** | 🟡 | M | Stub wired in Home (`showNotAvailableSnackBar`); storage TBD |
| 10 | **Auto-update — Windows msix** (`.appinstaller`) | 🟡 | S | Blocked by self-signed cert; needs trusted CA (Azure Trusted Signing or SignPath.io) |
| 12 | **Auto-update — macOS** | 🟡 | M | `flutter auto_updater` or `desktop_updater`; needs code signing |
| 13 | **iOS build** | 🟡 | XL | C++ `.a` static lib in Xcode, provisioning, entitlements |
| 14 | **Entity images** (format + storage decision) | 🟢 | L | Architecture TBD — filesystem / base64 / asset bundle |
| 15 | **Image picker in wizards** (Weapon / Sight / Ammo) | 🟢 | M | Blocked by #14 |
| 16 | **Tools Screen** | 🟢 | M | Opened from "More" on Home; composition TBD |
| 18 | **Code Quality #15** — apply naming conventions | 🟢 | L | Incremental during other refactors |
| — | **Android — Google Play** | 🟢 | XL | Store listing, review; signing ✅; app identifier + fingerprint registered ✅; in-app update API |
| — | **iOS — App Store** | 🟢 | XL | Provisioning, review, App Store Connect |
| — | ~~**Linux snap / flatpak**~~ | ~~🟢~~ | ~~L~~ | Замінено AppImage + zsync autoupdate ✅ |
| — | **Windows portable** | 🟢 | S | Zip bundle; update via "View Release" → GitHub (existing flow) |

---

## Done ✅

- [x] **Android keyboard overlap** — `resizeToAvoidBottomInset: false` on `_ScaffoldWithNav` in `router.dart`
- [x] **Unit symbols not localized** — `UnitConstrainedInputTile`, `AdjustmentDisplay` etc. audited; `localizedSymbol()`/`localizedLabel()` applied everywhere
- [x] **Settings — Legal links** — GitHub, Privacy Policy, Terms of Use, Changelog links in Settings → About / Links
- [x] **Filter panel** — My Weapon / My Sight / My Ammo lists; vendor / caliber / weight / focal-plane filters; `ExpansionTile` sheet with badges
- [x] **Auto-update — Android APK** — `ota_update` 7.x; ABI-aware APK selection (`Abi.current()`); progress bar + install flow; "View Release" fallback
- [x] **Auto-update — Linux AppImage** — appimageupdate with zsync via GitHub Releases; tested end-to-end
- [x] **Help Overlay** — per-screen markdown help dialogs (? button in AppBar); all screens covered
- [x] **Possible unhandled error** — `toEbcp()` StateError now handled with user feedback
- [x] **.a7p import/export** — `packages/a7p`; `A7pService` in `lib/core/services/`
- [x] **Profile export / import** — bottom sheet with format selection (.ebcp / .a7p); FilePicker with auto-detect
- [x] **Full backup** — Settings → "Export backup" / "Import backup" (.ebcp)
- [x] **Reticle Fullscreen Screen** — `ReticleViewScreen`: ZoomableView, barrel drum, target/reticle pickers, clicks settings
- [x] **RulerSelector** — implemented in `QuickActionsPanel`
- [x] **In-app update checker** — GitHub Releases API; bottom sheet; throttled 24 h; manual check in Settings
- [x] **Collection update checker** — GitHub commit SHA; cached `collection.json`; throttled 24 h; manual check in Settings
- [x] **Caliber mismatch action sheet** — blocks ammo apply on caliber mismatch; update ammo / update weapon / cancel
- [x] **Display & enter corrections in clicks** — Home, Tables, ReticleViewScreen, AmmoWizardScreen
- [x] **ammo.zeroOffset** — `zeroOffsetX/Y` fields in `Ammo`; UI in `AmmoWizardScreen`
- [x] **Localization EN / UA** — ~375 keys; all screens, wizards, collections, pickers covered
- [x] **Code Quality #1–#14** — SimpleConvertorVm/Screen, TwoColumnTableEditorScreen, WizardActionBar, home_vm split, ammo_wizard split, profile_card split, dialog helpers, ui_dimensions, SvgAssetPickerScreen, WizardNotifiers, naming conventions in CLAUDE.md
- [x] **Android APK build** — signing, release flavors, CI workflow
- [x] **Windows msix build** — signed, CI workflow
- [x] **CI/CD** — release workflow: all platforms, GitHub Release with assets
- [x] **Database resilience** — `_openStore` catches failures, re-inits fresh store, SnackBar warning

---

## Open sub-sections

### Ballistics & UI

- [ ] **Custom drag function editor** — `CustomDragTableEditor` is read-only; full editing, validation, saving pending *(🟢 Low — UX needs rethink first)*

---

### Entity Images

- [ ] Format and storage for `entity.image` (Weapon/Sight/Ammo) — TBD (filesystem/base64/asset)
- [ ] Image picker / camera UI in wizard screens (Weapon, Sight, Ammo)
- [ ] `SightWizardScreen._SightPlaceholder` — replace `Icon(IconDef.sight)` with SVG reticle from `reticleSvgProvider`

---

### Platform & Build

- [ ] **macOS** — build for macOS (FFI + code signing)
- [ ] **iOS** — build for iOS (C++ `.a` static lib in Xcode, provisioning)
- [ ] **Auto-update — Windows msix** — `.appinstaller` blocked by self-signed cert; trusted CA needed
- [ ] **Auto-update — macOS** — `flutter auto_updater` or `desktop_updater`
- [ ] **iOS ipa** — App Store only; no sideload autoupdates
- [ ] **Windows portable** — zip bundle; update via "View Release" → GitHub
- [ ] **Android — Google Play** — store listing, review; in-app update API

---

### UX / Features

- [ ] **Notepad** — Note button on Home (currently `showNotAvailableSnackBar`); storage TBD
- [ ] **Tools Screen** — "More" button on Home; composition TBD
- [ ] **Help screenshots** — pending for: `ammoWizard` (7 images), `weaponWizard`, `sightWizard`, `myAmmo`, `mySights`, collection screens

---

### Code Quality

- [ ] **#15 Apply naming conventions** — incremental during other refactors; see CLAUDE.md
