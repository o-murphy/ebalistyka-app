# Beta Features

> **Doc #7 — Status: BACKLOG 📋**  
> Features for after alpha release. Architecture TBD for each item.

---

## What to do next

Priority: 🔴 High · 🟡 Medium · 🟢 Low  
Effort: S < 1 day · M 2–3 days · L 1+ week · XL multi-week

| # | Item | Priority | Effort | Notes |
|---|------|----------|--------|-------|
| ~~1~~ | ~~**Android keyboard overlap** bug fix~~ | ~~🔴~~ | ~~S~~ | `resizeToAvoidBottomInset: false` on `_ScaffoldWithNav` in `router.dart` ✅ |
| ~~2~~ | ~~**Unit symbols not localized** in some widgets~~ | ~~🔴~~ | ~~S~~ | ~~`UnitConstrainedInputTile`, `AdjustmentDisplay` etc. still use `unit.symbol` — audit all remaining `.symbol`/`.label` call sites~~ |
| 3 | **Custom drag function editor** — full edit/save | 🔴 | M | Read-only scaffold already exists (`CustomDragTableEditor`) |
| 4 | **`_SightPlaceholder` → SVG reticle** | 🟢 | S | Replace `Icon(IconDef.sight)` with SVG from `reticleSvgProvider`; pattern in `_RiflePlaceholder` |
| ~~5~~ | ~~**Settings — Legal links**~~ | ~~🟡~~ | ~~S~~ | Privacy Policy, Terms of Use, Changelog links in Settings ✅ |
| 6 | **macOS build** | 🟡 | M | CI pattern exists from Linux/Windows; FFI + code signing |
| 7 | **Notepad** | 🟡 | M | Stub wired in Home (`showNotAvailableSnackBar`); storage TBD |
| ~~8~~ | ~~**Filter panel** (Weapon / Sight / Ammo)~~ | ~~🟡~~ | ~~L~~ | ~~Fully spec'd — vendor/weight/focal-plane, badges, `ExpansionTile` sheet~~ |
| 9 | **Auto-update — Android APK** (`ota_update`) | 🟡 | M | OTA update via GitHub Releases; ~~background check~~ + install flow |
| 10 | **Auto-update — Windows msix** (`.appinstaller`) | 🟡 | S | `.appinstaller` currently invalid — debug and fix |
| ~~11~~ | ~~**Auto-update — Linux AppImage** (zsync)~~ | ~~🟡~~ | ~~S~~ | tested end-to-end ✅ |
| 12 | **Auto-update — macOS** | 🟡 | M | `flutter auto_updater` or `desktop_updater`; needs code signing |
| 13 | **iOS build** | 🟡 | XL | C++ `.a` static lib in Xcode, provisioning, entitlements |
| 14 | **Entity images** (format + storage decision) | 🟢 | L | Architecture TBD — filesystem / base64 / asset bundle |
| 15 | **Image picker in wizards** (Weapon / Sight / Ammo) | 🟢 | M | Blocked by #14 |
| 16 | **Tools Screen** | 🟢 | M | Opened from "More" on Home; composition TBD |
| 17 | **Help Overlay** | 🟢 | M | Coach marks on first run; library TBD (`tutorial_coach_mark`) |
| 18 | **Code Quality #15** — apply naming conventions | 🟢 | L | Incremental during other refactors |
| ~~19~~ | ~~**Possible unhandled error**~~ | ~~🔴~~ | ~~L~~ | ~~toEbcp() may raise a StateError with no feedback to usesr~~ |
| — | **Android — Google Play** | 🟢 | XL | Store listing, review, ~~signing~~, in-app update API |
| — | **iOS — App Store** | 🟢 | XL | Provisioning, review, App Store Connect |
| — | **Linux snap / flatpak** | 🟢 | L | Sandboxed distribution with autoupdates |
| — | **Windows portable** | 🟢 | M | Zip bundle with autoupdate support |

---

## Done ✅

- [x] **.a7p import/export** — `packages/a7p` (`A7pFile`, `A7pConverter`, `A7pValidator`, proto-scheme); `A7pService` in `lib/core/services/`
- [x] **Profile export** — bottom sheet with format selection: .ebcp / .a7p
- [x] **Profile import** — single FilePicker (.ebcp + .a7p), auto-detect by extension
- [x] **Full backup** — Settings → "Export backup" / "Import backup" (.ebcp)
- [x] **Reticle Fullscreen Screen** — `ReticleViewScreen`: `ZoomableView`, barrel drum adjustments, target/reticle pickers, clicks settings
- [x] **RulerSelector** — implemented in `QuickActionsPanel`
- [x] **Settings — Legal links** — GitHub, Privacy Policy, Terms of Use, Changelog links in Settings → About / Links
- [x] **In-app update checker** — GitHub Releases check on startup (throttled 24 h); bottom sheet with "View release" / "Open in Play Store"; manual check in Settings → About
- [x] **Collection update checker** — GitHub commit SHA check on startup (throttled 24 h); cached `collection.json` stored alongside ObjectBox; `builtinCollectionProvider` falls back to bundled asset; Settings → Collection shows SHA (7 chars) + manual update button
- [x] **Caliber mismatch action sheet** — blocks ammo apply when caliber differs from weapon; action sheet offers: update ammo caliber, update weapon caliber, or cancel; triggered on both ammo select and ammo edit open

---

## Ballistics & UI

- [x] **Display corrections in clicks** — Home Page 1 (AdjPanel) ✅, Tables screen ✅, ReticleViewScreen ✅
- [x] **Enter corrections in clicks** — ReticleViewScreen ✅, AmmoWizardScreen - allow to enter corrections in clicks
- [x] **ammo.zeroOffset** — fields `zeroOffsetX` / `zeroOffsetY` / `zeroOffsetUnitX` / `zeroOffsetUnitY` in `Ammo`, UI in `AmmoWizardScreen`, taking into account in calculations
- [ ] **Custom drag function editor** — complete custom drag function editor (`CustomDragTableEditor`): now read-only by default; add full editing, validation, saving
- [x] **Caliber mismatch action sheet** — see Done ✅ above
- [x] **Known bug** - overlap on android on keyboard open

---

## Filter Panel

- [x] **Filter panel** — My Weapon / My Sight / My Ammo lists and their collection screens (currently filter button is a stub)
  - **Weapon** — filter by: vendor
  - **Ammo** filter by: diameter (caliber, sorted checkboxes), vendor, bullet weight (range via `UnitConstrainedField` or kinda `offsets_edit.dart`)
    - For now my_ammo and cartridges/bullets collections are always filtered by diameter of weapon, but after filter implementation I wanna display all items but with diameter by weapon filter enabled by default on screen opening. 
  - **Sight** — filter by: vendor, focal plane (FFP / SFP)
  - **UI** — top sheet or side drawer; scrollable `ListView` with `ExpansionTile` spoilers per category
    - Categorical values: checkboxes for multi-select
    - Weight: `UnitConstrainedField` (min / max range)
    - Each value tile shows a `Badge` with the item count matching that value

---

## Entity Images

- [ ] Format and storage for `entity.image` (Weapon/Sight/Ammo) — TBD (filesystem/base64/asset)
- [ ] Image picker / camera UI in wizard screens (Weapon, Sight, Ammo)
- [ ] `SightWizardScreen._SightPlaceholder` — replace `Icon(IconDef.sight)` with SVG reticle from `reticleSvgProvider` (similar to `_RiflePlaceholder` in WeaponWizard)

---

## Code Quality

- [~] **Optimization and restructuring** — unification of components, reusable components; audit of common patterns between screens
  - See [7.1.Code_Quality.md](7.1.Code_Quality.md) for full breakdown
  - [x] #1 `SimpleConvertorVm` base class — 5 converter VMs unified (−600 LOC)
  - [x] #2 `SimpleConvertorScreen` — 5 converter screens unified (−270 LOC)
  - [x] #3 `TwoColumnTableEditorScreen` — multi-BC + powder sens table editors unified (−545 LOC)
  - [x] #4 `WizardActionBar` + `WizardNameField` + `WizardFormMixin` — wizard boilerplate unified (−155 LOC)
  - [x] #5 `home_vm.dart` split → `home_ui_state.dart` + `home_builders.dart` + slim notifier
  - [x] #6 `ammo_wizard_screen.dart` split → `ammo_wizard_parsers.dart` + `ammo_wizard_notifier.dart`; 77 tests
  - [x] #7 `profile_card.dart` split → `ProfileCardHeader` + `ProfileSectionTile` + `ProfileActionsBar` (−360 LOC)
  - [x] #8 Dialog/sheet helpers unified (`confirm_dialog`, `action_sheet`, `snackbars`)
  - [x] #9 `ui_dimensions.dart` + `TileDivider`/`SectionDivider` widgets (−47 divider calls)
  - [x] #10 `SvgAssetPickerScreen<T>` generic — reticle/target/svg pickers as 22-line wrappers
  - [x] #11 `WeaponWizardNotifier` + `SightWizardNotifier` — all `setState` removed from both screens
  - [x] #12 `AdjustmentsDisplayPanel` disabled state — `_buildEmpty` stub replaced; test updated
  - [x] #13 `_BcSection` + `_DragModelSection` extracted as standalone widgets in `ammo_wizard_screen.dart`
  - [x] #14 Naming conventions documented in `CLAUDE.md` (widget classes, providers, localization rules)
  - [ ] #15 Apply naming conventions (#14) to existing codebase — 📋 Backlog (incremental)

---

## Localization (EN / UA)

- [x] ARB + `flutter_localizations`
- [x] Languages: Ukrainian (main) + English — ~375 keys, EN = UK (fully in sync)
  - See [7.2.Localization.md](7.2.Localization.md) — all 12 checklist items ✅ Done
  - [x] **Bug: unit symbols not localized in all widgets** — see [7.2.Localization.md §5](7.2.Localization.md#5-full-widget-audit--remaining-symbol-call-sites-); `unit_picker_button.dart` still uses `current.symbol`
  - [x] Settings screen (+ sub-screens: units, adjustments)
  - [x] Convertors screen (all sub-screens)
  - [x] Conditions screen
  - [x] HTML trajectory report
  - [x] Tables screen
  - [x] Tables config screen
  - [x] Trajectory table widget
  - [x] Details table widget
  - [x] Home screen (main screen, page labels, condition indicators, wind direction)
  - [x] Shot details screen (`ShotInfoScreen`)
  - [x] Unit pickers — `localizedLabel()` + `localizedSymbol()` extension, all formatted values via `UnitFormatterImpl`
  - [x] Profiles screen (`my_profiles_screen`)
  - [x] Ammo wizard
  - [x] Collection screens — ammo tile body, weapon tile body, sight tile body (defaultLabel)
  - [x] Sight wizard (FFP/SFP/LWIR focal plane, default reticle label)
  - [x] Reticle view screen (default target/reticle labels)
  - [x] Weapon wizard

---

## Platform & Build

- [x] **Android APK** — configure the build (signing, release flavors)
- [ ] **macOS** — build for macOS
- [ ] **iOS** — build for iOS (C++ `.a` static lib in Xcode)
- [x] **Windows** — package with msix and sign the application for distribution
- [x] **CI/CD** — release workflow: builds all platforms, publishes GitHub Release with all assets
- [~] **Auto-update** — auto-update (desktop and mobile)
  - [x] **In-app update check** — GitHub Releases API; bottom sheet; throttled 24 h; manual check in Settings
  - [x] **Collection update check** — GitHub commits API; cached collection.json; throttled 24 h; manual check in Settings
  - [x] **Linux AppImage** — appimageupdate with zsync via github releases — tested end-to-end ✅
  - [ ] **Windows msix** — `.appinstaller` blocked by self-signed certificate (no trusted CA); workaround without paid cert TBD
  - [ ] **Android apk** — ota_update
  - [ ] **macOS** — flutter auto_updater or modern desktop_updater
  - [ ] **iOS ipa** — only app-store, no autoupdates via sideload
- [ ] Possibly in future
  - [ ] **Linux portable/snap/flatpak** - with autoupdates?
  - [ ] **Windows portable** - add autoupdates?
  - [ ] **Android apk** — GooglePlay, already got registered app identifier and sign fingerprint
  - [ ] **iOS ipa** — App Store ?
---

## Reliability

- [x] **Database resilience** — `_openStore` catches open failures, deletes `data.mdb`/`lock.mdb`, re-inits fresh store; SnackBar warning shown only when prior data existed

---

## UX / Features

- [ ] **Notepad** — Note button on Home (currently `showNotAvailableSnackBar`)
- [~] **Help Overlay** — Coach marks on first run or upon user request; library TBD (e.g. `tutorial_coach_mark`)
  - Added help dialogs for screens: `shot info`, `my profiles`
  - Added help dialogs for collection screens: `ammo`, `weapon` and `sights` collections
  - Added help dialogs for user's data screens: `my ammo`, `my sights` screens
  - Added help dialogs for data table editor screens: `multi bc`, `custom drag` and `powder sensitivity`
  - Added help dialogs for wizard screens: `sight`, `weapon`
  - [ ] ammo wizard screen
- [ ] **Tools Screen** — Opened from "More" button on Home; composition TBD
- [ ] **Settings — Legal links** — Privacy Policy, Terms of Use, Changelog
